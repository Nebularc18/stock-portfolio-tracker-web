name: Docker Publish

on:
  push:
    branches:
      - main
    tags:
      - v*
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Normalize image name
        run: echo "IMAGE_NAME=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Compute publish metadata
        id: publish
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            TRACKING_TAG="${GITHUB_REF_NAME}"
            RELEASE_CHANNEL="release"
          else
            BUILD_STAMP="$(date -u +%Y%m%d-%H%M%S)"
            SHORT_SHA="${GITHUB_SHA::7}"
            TRACKING_TAG="build-${BUILD_STAMP}-${SHORT_SHA}"
            RELEASE_CHANNEL="edge"
          fi

          {
            echo "tracking_tag=${TRACKING_TAG}"
            echo "release_channel=${RELEASE_CHANNEL}"
          } >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha
            type=raw,value=${{ steps.publish.outputs.tracking_tag }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
          labels: |
            org.opencontainers.image.title=Stock Portfolio Tracker Web
            org.opencontainers.image.description=React and FastAPI stock portfolio tracker packaged as a multi-architecture container image.
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            io.stock-portfolio-tracker.release-channel=${{ steps.publish.outputs.release_channel }}
            io.stock-portfolio-tracker.tracking-tag=${{ steps.publish.outputs.tracking_tag }}

      - name: Build and optionally push multi-arch image
        id: build
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Publish image summary
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          METADATA_JSON: ${{ steps.meta.outputs.json }}
          RELEASE_CHANNEL: ${{ steps.publish.outputs.release_channel }}
          TRACKING_TAG: ${{ steps.publish.outputs.tracking_tag }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          metadata = json.loads(os.environ["METADATA_JSON"])
          tags = metadata.get("tags", [])
          payload = {
              "image": os.environ["IMAGE_NAME"],
              "tracking_tag": os.environ["TRACKING_TAG"],
              "release_channel": os.environ["RELEASE_CHANNEL"],
              "digest": os.environ["DIGEST"],
              "git_ref": os.environ["GITHUB_REF"],
              "git_sha": os.environ["GITHUB_SHA"],
              "tags": tags,
          }

          summary_lines = [
              "## Published image",
              "",
              f"- Image: `{payload['image']}`",
              f"- Tracking tag: `{payload['tracking_tag']}`",
              f"- Channel: `{payload['release_channel']}`",
              f"- Digest: `{payload['digest']}`",
              f"- Git ref: `{payload['git_ref']}`",
              f"- Git SHA: `{payload['git_sha']}`",
              "",
              "### Tags",
              "",
          ]
          summary_lines.extend(f"- `{tag}`" for tag in tags)
          summary_path = Path(os.environ["GITHUB_STEP_SUMMARY"])
          summary_path.write_text("\n".join(summary_lines) + "\n", encoding="utf-8")
          PY
